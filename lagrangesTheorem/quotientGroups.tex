\documentclass{ximera}

\input{../preamble.tex}

\author{Bart Snapp}

\title{Quotient groups}

\begin{document}
\begin{abstract}
  We introduce quotient groups.
\end{abstract}
\maketitle

\begin{definition}
  A subgroup $N\subgp G$ is a \dfn{normal} subgroup if
  \[
  aN = Na,
  \]
  meaning every left coset is equal to the right coset involving the
  same group element. In this case we write $N\normal G$. \index{normal@$\normal$}
\end{definition}

Row of Cayley table has same elements as column (perhaps in different order).



\begin{lemma}[Normal subgroup criterion]
  A subgroup $N\subgp G$ is normal if and only if for all $n\in N$ and
  all $g\in G$,
  \[
  gng^{-1}\in N.
  \]
  \begin{proof}
    $(\Rightarrow)$

    $(\Leftarrow)$
  \end{proof}
\end{lemma}



\[
\int f(x) \d x + C
\]


\begin{theorem}[A subgroup of index two is normal]
\end{theorem}


\end{document}
